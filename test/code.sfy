((
    const<int>
    {
        x <- 10 + 9, y <- 3,
        v,
        z <- 7 + 3 * (10 / (12 / (3 + 1) - 1)) / (2 + 3) - 5 - 3 + (8)
    };

    name <- a;

    game <- 12 + 12;

    voyou <- name;

    //val<auto> n <- 34;
))