((
    //? Defined a const integer
    val<int> //TODO: Check a const declaraion 
    {
        x <- 10 + 9,
        y <- 3.98,
        z <- 7 + 3,
    };

    val<float> l <- 3.98;

    //? Assign a var {name} to value of var {a}
    x <- z + 2;

    //? Assign a var {game} to a math [expr]
    z <- 12.23;
 
    //? Comment on multiple lines
    /****

        STYLFY C.O.D.E
    
    ****/
    //? Comment on a line
    x <- 5 + + (-12*2) --- l;
    
    val<auto> n <- 34;

    // (int:a, int:b)

    //? Function declaration
    func<int> add(int:x, int:y)
    {
        val<float> result <- 0 + x + n;
        // return a + b;
    };

    // this.add();

    // struct<T> User
    // {
    //     contrc(T:name)
    //     {
    //         this.name <- name;
    //     }
    // }

    // struct Person : User<T>
    // {
    //     contrc(string:name) : super(name) {}
    // }

    // val<auto> user <- User<string>();
))